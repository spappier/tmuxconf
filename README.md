# tmuxconf

```
git clone --recursive git@github.com:spappier/tmuxconf.git ~/path/to/tmuxconf
ln -s ~/path/to/tmuxconf/tmux.conf ~/.tmux.conf
```

### or

```
curl -o ~/.tmux.conf https://raw.githubusercontent.com/spappier/tmuxconf/master/tmux.conf
```
